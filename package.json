{
    "main": "dist",
    "private": true,
    "scripts": {
        "test": "npm run build && npm run lint",
        "lint": "eslint --ext .js,.jsx,.ts,.tsx src",
        "watch": "tsc -p . -w --preserveWatchOutput --inlineSourceMap",
        "start": "tabris serve -a -w"
    },
    "dependencies": {
        "base-64": "^1.0.0",
        "jszip": "^3.10.0",
        "path": "^0.12.7",
        "tabris": "~3.8.0",
        "components-tabris": "https://github.com/everskyblue/components-tabris.git"
    },
    "devDependencies": {
        "@types/base-64": "^1.0.0"
    }
}
